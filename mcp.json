{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "open-market-data": {
      "type": "stdio",
      "command": "node",
      "args": [
        "${PLUGIN_ROOT}/dist/mcp-cli.js"
      ],
      "cwd": "${PLUGIN_ROOT}"
    }
  }
}
